# HERITIA frontend — copy to .env / .env.production

# Local (Vite proxy → backend :8001)
VITE_API_URL=/api
VITE_APP_URL=http://localhost:5174

# --- Production (N2 Single Ingress → heritia.neriacorp.com) ---
# API same-origin via N2 proxy (recommended)
# VITE_API_URL=/api
# VITE_APP_URL=https://heritia.neriacorp.com
